import Popup from "./Popup.js";

export default class PopupWithImage extends Popup {
  constructor({ name, link }) {
    this._caption = name;
    this._alt = name;
    this._image = link;
  }

  open({data}){
    this.
  }
}
